- chat_history:
  question: When does the contract for ABC corp expire?
  tool_descriptions: |
    1. document_answer_tool_contracts_report: Pass the user's question, after rewriting it to be self-contained based on chat history, as input directly to this tool. Use this tool if you think the answer is 
    likely to come from one or a few of these documents. This document type is the Master Service Agreement Contracts.

    2. query_contracts_report: Pass the user's question, after rewriting it to be self-contained based on chat history, as input directly to this tool. Use this tool if you think the answer is 
    likely to come from one or a few of these documents. This document type is the Master Service Agreement Contracts.

    3. chat_bot: Responds to greetings, small talk, or questions that can be directly answered from the chat history.

  intermediate_steps: |
    Tool Name: query_contracts_report
      input: SELECT Expiry_Date FROM Contracts_Report WHERE Name LIKE '%abc%'
      output: Looks like you passed in a SQL query. This tool takes natural language inputs, and automatically translates them to SQL queries. Please try passing in a natural language query.
  cited_answer_json: |
    {
      "source": "report_answer_tool_contracts_report",
      "answer": "Looks like you passed in a SQL query. This tool takes natural language inputs, and automatically translates them to SQL queries. Please try passing in a natural language query.",
      "is_final": false
    }
